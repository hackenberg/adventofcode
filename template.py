from utils import *


def p1(text: str) -> any:
    in1 = parse(text)
    show_items("Part 1: parsed input", in1, 10)
    #import IPython; IPython.embed(colors="neutral")
    #import ipdb; ipdb.set_trace()


def p2(text: str) -> any:
    in2 = parse(text)
    #show_items("Part 2: parsed input", in2, 10)
